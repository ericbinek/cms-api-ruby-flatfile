name: test

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.4'
      - run: ruby -e "Dir.glob('test/*_test.rb').sort.each { |f| require File.expand_path(f) }"
